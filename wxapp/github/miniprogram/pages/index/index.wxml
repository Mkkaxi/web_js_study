
  <view class="trends page">
    <!-- component 能力 设计props -->
    <!-- search 组件复用 -->
    <search custom-class='elevation' placeholder="Search Users or Repositories" bind:search="onSearch"></search>
    <trend repo='{{item}}' wx:for="{{trends}}" wx:key="{{index}}"></trend>
  </view>
